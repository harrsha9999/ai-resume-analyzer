# Copy to .env. All values are optional — the app works offline without them.

PORT=5000

# If set, the analyzer uses the OpenAI API for richer suggestions.
# If empty, it falls back to a deterministic local heuristic engine.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
