services:
  - type: web
    name: ai-resume-analyzer-harsha
    runtime: node
    plan: free
    buildCommand: npm ci
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: "20"
